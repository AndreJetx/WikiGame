{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.32.0",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const f of ['package-lock.json','yarn.lock']) try { fs.unlinkSync(f); } catch {} if (!String(process.env.npm_config_user_agent||'').startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "node ./scripts/dev.mjs",
    "dev:apps": "pnpm -r --parallel --filter \"./artifacts/**\" --if-present run dev",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:push": "pnpm --filter @workspace/db push",
    "db:ensure-schema": "pnpm --filter @workspace/db ensure-schema",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present test"
  },
  "dependencies": {
    "@vercel/functions": "^3.2.0",
    "@workspace/agent-site": "workspace:*"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3",
    "vitest": "^3.2.4"
  }
}
